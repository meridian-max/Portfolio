export const metadata = {
  title: "Notes",
  description:
    "Engineering notes from the GreedUp studio — what we have learned shipping booking SaaS, AI products, and platform infrastructure.",
};

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
