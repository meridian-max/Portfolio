export const metadata = {
  title: "Notes",
  description:
    "Source-backed notes derived from Meridian Works public builds.",
};

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
